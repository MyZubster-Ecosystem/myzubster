name: Deploy to Vercel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: package.json
      - name: Validate Vercel token
        id: vercel_token
        shell: bash
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::warning::VERCEL_TOKEN is not configured; skipping CLI deployment. Configure the repository secret to enable Vercel deploys."
          else
            echo "available=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Install Vercel CLI
        if: steps.vercel_token.outputs.available == 'true'
        run: npm install -g vercel
      - name: Link Vercel project
        if: steps.vercel_token.outputs.available == 'true'
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel link --yes --project my-zubster-app --scope Myzubster --token "$VERCEL_TOKEN"
      - name: Enable Zorgax OpenAI routing in Production
        if: steps.vercel_token.outputs.available == 'true' && contains(github.event.head_commit.message, '[enable-zorgax-openai]')
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: printf 'true\n' | vercel env add ZORGAX_ASTRA_ENABLED production --force --scope Myzubster --token "$VERCEL_TOKEN"
      - name: Enable Vercel Web Analytics
        if: steps.vercel_token.outputs.available == 'true'
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel project web-analytics my-zubster-app --scope Myzubster --token "$VERCEL_TOKEN" --format json
      - name: Deploy to Vercel
        if: steps.vercel_token.outputs.available == 'true'
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: vercel --prod --token "$VERCEL_TOKEN"
