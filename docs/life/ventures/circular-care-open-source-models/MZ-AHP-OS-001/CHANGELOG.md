# MZ-AHP-OS-001 — Changelog

## 0.3-draft — Prototype Pack

- added draft target masses to the BOM while keeping measured mass fields empty;
- added packaging specification;
- added traceable prototype-build notes;
- added reproducible internal bench-screening protocol;
- added circular-flow evidence schema;
- added evidence index;
- preserved `DESIGN_REFERENCE` state and explicit non-production / non-certification boundaries.

Draft total allocated mass target: **36.0 g per functional unit**, including 1.0 g packaging allocation. This is `DESIGN_TARGET_NOT_VALIDATED`, not a measured or approved product specification.

## 0.2-draft — Geometry and material architecture

- added `BOM.csv`;
- added `material-passport.json`;
- added `dimensions.svg`;
- added `layer-stack.svg`;
- established medium reference envelope and evidence gates.

## 0.1 — Open reference architecture

- established model ID `MZ-AHP-OS-001`;
- documented common AHP layer vocabulary;
- established evidence-first and design-for-recovery principles.

## State boundary

A changelog entry records design work only. It does not upgrade the model to `PROTOTYPE_BUILT`, `TESTED`, `PILOT_EVIDENCED` or `VERIFIED_CIRCULAR_OUTCOME` without the corresponding evidence.