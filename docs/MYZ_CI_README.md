# MYZ CI entrypoint

Use the repository-wide CI plus `.github/workflows/myz-ledger-contract.yml` for core verification. Use the Marketplace cross-repository/staging workflows for end-to-end compatibility. Do not treat CI success as proof of a live MYZ market or external settlement support.
