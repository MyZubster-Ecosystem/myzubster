const ledgerEl=document.getElementById('ledger');const searchEl=document.getElementById('search');const filterEl=document.getElementById('stateFilter');let records=[];function render(){const q=searchEl.value.trim().toLowerCase();const state=filterEl.value;const filtered=records.filter(r=>(state==='all'||r.state===state)&&(!q||[r.title,r.type,r.claim,r.note,r.state].join(' ').toLowerCase().includes(q)));ledgerEl.innerHTML=filtered.map(r=>`<article class="entry"><div><div class="state ${r.state}">${r.state}</div><div class="meta">${r.type}</div></div><div><h3>${r.title}</h3><p>${r.claim}</p><p class="meta">${r.note||''}</p></div><div class="evidence"><a href="${r.evidence_url}" rel="noreferrer">${r.evidence_label} ↗</a></div></article>`).join('')||'<p>No records match this filter.</p>';}fetch('ledger.json').then(r=>r.json()).then(data=>{records=data.records||[];render();}).catch(()=>{ledgerEl.innerHTML='<p>Ledger data could not be loaded. Open ledger.json directly for the canonical static record.</p>';});searchEl.addEventListener('input',render);filterEl.addEventListener('change',render);