# N4K48 — MyZubster Metaverse character

Status: `CONFIGURATION_PREPARED`

## Participant-authorized configuration

The participant confirmed the following configuration on **2026-08-31**:

- Character name: `N4K48`
- Archetype: `Explorer`
- Starting world: `Neon Plaza` (`neon-plaza`)
- GitHub linkage: confirmed for the GitHub identity already used in the MyZubster pilot
- GitHub login: `nicolaususnicola-lgtm`

## Activation path

The runtime configuration is implemented by:

```text
scripts/metaverse/configure-n4k48.js
```

The script deliberately does **not** create a guest or unverified identity. It resolves the participant through the existing MyZubster GitHub-linked account and only then upserts the account-linked Metaverse character.

Until the participant completes the normal authenticated MyZubster GitHub account-link / OAuth flow, this record must not be presented as a verified live account-linked character.

## Runtime target

```text
GitHub-linked MyZubster account
        ↓
N4K48 / Explorer
        ↓
Neon Plaza
```

Expected character fields after successful configuration:

- `characterName`: `N4K48`
- `displayName`: `N4K48`
- `archetype`: `explorer`
- `worldId`: `neon-plaza`
- `identityStatus`: `account-linked`
- `createdFrom`: `account-github`

## Privacy and identity boundary

No email address, password, token, 2FA code, private key, identity document, home address, or other unnecessary private data is stored in this Metaverse record.

GitHub identity confirmation by email authorizes the requested linkage configuration, but cryptographic/account verification remains the responsibility of the normal authenticated MyZubster GitHub linkage flow.
